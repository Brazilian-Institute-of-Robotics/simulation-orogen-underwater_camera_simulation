# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(underwater_camera_simulationTaskLib)
ADD_LIBRARY(${UNDERWATER_CAMERA_SIMULATION_TASKLIB_NAME} SHARED
    ${UNDERWATER_CAMERA_SIMULATION_TASKLIB_SOURCES})
add_dependencies(${UNDERWATER_CAMERA_SIMULATION_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${UNDERWATER_CAMERA_SIMULATION_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${UNDERWATER_CAMERA_SIMULATION_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${UNDERWATER_CAMERA_SIMULATION_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${UNDERWATER_CAMERA_SIMULATION_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${UNDERWATER_CAMERA_SIMULATION_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${UNDERWATER_CAMERA_SIMULATION_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${UNDERWATER_CAMERA_SIMULATION_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${UNDERWATER_CAMERA_SIMULATION_TASKLIB_HEADERS}
    DESTINATION include/orocos/underwater_camera_simulation)

